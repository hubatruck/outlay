<ul uk-accordion>
  <li>
    <a class="uk-accordion-title"><span uk-icon="lifesaver"></span>&nbsp;{{ __('Usage help') }}</a>
    <div class="uk-accordion-content">
      <ul>
        <li>{{ __('Use the upper search box to filter all columns') }}.</li>
        <li>{{ __('Filter individual columns using the input boxes found at the bottom of the table') }}.</li>
        <li>
          {!! __('Hold down <code>SHIFT</code>, while clicking on column headers to sort by multiple columns at the same time') !!}
          .
        </li>
        <li>{{ __('Columns can be reordered, by clicking and holding a column header, and then dragging it') }}.</li>
      </ul>
    </div>
  </li>
</ul>
